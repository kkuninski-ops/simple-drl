/*
 * Circuit 4 — S32 → Radio ILL+ brightness
 *
 * За тази схема НЕ е нужен ATtiny85.
 * Използва се само LM358 (op-amp) + IRF9540N (P-ch MOSFET).
 * Няма код за програмиране — чисто аналогова схема.
 *
 * Виж README.md за схемата.
 */

// Този файл е оставен за справка.
// Circuit 4 е аналогова схема — не изисква микроконтролер.
